import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Reporte de Performance — Trading Journal",
}

export default function ExportLayout({ children }: { children: React.ReactNode }) {
  return children
}
